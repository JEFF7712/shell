#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
PROJECT_ROOT="$(cd "${SCRIPT_DIR}/.." && pwd)"

exec node "${PROJECT_ROOT}/node_modules/tsx/dist/cli.mjs" "${PROJECT_ROOT}/src/cli.ts" "$@"
